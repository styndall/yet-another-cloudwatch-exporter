apiVersion: v1alpha1
discovery:
  jobs:
    - type: AWS/IPAM
      regions:
        - us-east-1
      metrics:
        - name: SubnetIPUsage
          statistics: [Maximum]
          period: 300
          length: 300
